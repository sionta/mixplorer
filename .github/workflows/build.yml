name: Deploy Theme

on:
  push:
    tags:
      - 'v*'
    branches:
      - 'master'
  pull_request:
    branches:
      - 'master'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # allows the action to create a release.
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        with:
          python-version: '3.9'
          check-latest: true
        uses: actions/setup-python@v4
      - name: Build Package
        run: |
          sudo apt-get install -y librsvg2-bin
          python ./tools/build.py -f -a pink
          python ./tools/build.py -f -a purple
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: dracula-artifact
          path: ./build/*.*
        if: github.ref == 'refs/heads/master' || github.event_name == 'pull_request'
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python ./tools/changelog.py -n > ./release_notes.txt
          gh release create ${{ github.ref_name }} $(find ./build -type f) \
            --title ${{ github.ref_name }} \
            --notes-file ./release_notes.txt \
            --draft=false \
            --prerelease=false \
            --verify-tag
